# Union Basics

This project demonstrates how a union works in C. All members of a union share the same memory location, so only one member holds a valid value at a time.

## What I Learned
- How to define and use a union in C
- How all union members share the same memory space
- How assigning to one member overwrites the others
- How to check the size of a union using sizeof
